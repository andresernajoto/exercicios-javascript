// Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente.

const calcularExpoent = function(base, expoente) {
    return Math.pow(base, expoente)
}

console.log(calcularExpoent(5, 3))