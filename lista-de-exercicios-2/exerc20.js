const areaDoTriangulo = (base, altura) => {
    const formula = base * altura / 2

    return console.log(formula.toFixed(2))
}

areaDoTriangulo(10, 15)
areaDoTriangulo(7, 9)
areaDoTriangulo(9.25, 13.1)